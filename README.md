# News Portal

Веб-приложение новостного портала на Django с поддержкой новостей, статей, категорий, подписок и автоматических уведомлений.

## 🚀 Установка и запуск

### 1. Создание виртуального окружения и установка зависимостей

```bash
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

> **Примечание**: Для Linux/Mac используйте `.venv/bin/python` вместо `.\.venv\Scripts\python.exe`

### 2. Установка Redis

#### Windows:
1. Скачайте Redis для Windows с [GitHub Releases](https://github.com/microsoftarchive/redis/releases) или используйте WSL2
2. Запустите `redis-server.exe`

#### Linux (Ubuntu/Debian):
```bash
sudo apt-get update
sudo apt-get install redis-server
sudo systemctl start redis-server
```

#### macOS:
```bash
brew install redis
brew services start redis
```

Проверьте, что Redis запущен:
```bash
redis-cli ping
# Должен вернуть: PONG
```

### 3. Применение миграций

```bash
.\.venv\Scripts\python.exe manage.py migrate
```

### 4. Создание суперпользователя (опционально)

```bash
.\.venv\Scripts\python.exe manage.py createsuperuser
```

### 5. Запуск сервера разработки

Для полноценной работы необходимо запустить 3 процесса:

#### 5.1. Django сервер (в первом терминале)
```bash
.\.venv\Scripts\python.exe manage.py runserver
```

#### 5.2. Celery Worker (во втором терминале)
```bash
.\.venv\Scripts\python.exe -m celery -A config worker --loglevel=info
```

#### 5.3. Celery Beat - планировщик задач (в третьем терминале)
```bash
.\.venv\Scripts\python.exe -m celery -A config beat --loglevel=info
```

> **Для Linux/Mac**: Используйте `.venv/bin/celery` вместо `.\.venv\Scripts\python.exe -m celery`

> **Важно**: Убедитесь, что Redis запущен перед запуском Celery!

После запуска проект будет доступен по адресу:
- **Главная страница**: [http://127.0.0.1:8000/](http://127.0.0.1:8000/)
- **Новости**: [http://127.0.0.1:8000/news/](http://127.0.0.1:8000/news/)
- **Админ-панель**: [http://127.0.0.1:8000/admin/](http://127.0.0.1:8000/admin/)

---

## 📋 Основная функциональность

### Новости и Статьи

Проект поддерживает два типа контента:

#### Новости (`/news/`)
- **Список новостей** (`/news/`)
  - Отображает заголовок (с цензурой), дату публикации, первые 124 символа текста
  - Пагинация (10 записей на страницу)
  - Сортировка по дате (новые первыми)

- **Детальная страница** (`/news/<id>/`)
  - Полный текст новости с применением цензуры
  - Дата публикации в формате `дд.мм.гггг`
  - Информация об авторе

- **Поиск новостей** (`/news/search/`)
  - Фильтрация по названию, автору, дате публикации
  - Расширенные возможности поиска

- **Создание/Редактирование/Удаление**
  - Требуется авторизация и права доступа
  - Лимит: не более 3 публикаций в сутки на пользователя
  - Автоматическое назначение автора из текущего пользователя

#### Статьи (`/news/articles/`)
- Аналогичная функциональность как у новостей
- Отдельные страницы списка и детального просмотра
- Независимое управление от новостей

### Категории и подписки

- **Просмотр категорий** (`/news/category/<id>/`)
  - Список всех постов в категории
  - Информация о подписке

- **Подписка на категории**
  - Авторизованные пользователи могут подписываться/отписываться
  - Автоматические email-уведомления при новой публикации в категории

### Аутентификация и авторизация

- **Регистрация и вход**
  - Стандартная регистрация через email
  - OAuth через Google и Yandex
  - Требуется подтверждение email для стандартной регистрации

- **Права доступа**
  - Создание постов: требует права `news.add_post`
  - Редактирование: требует права `news.change_post`
  - Удаление: требует права `news.delete_post`

### Email-уведомления

- **Мгновенные уведомления (через Celery)**
  - При создании нового поста все подписчики категорий получают email
  - Персональные письма с ссылкой на новый пост
  - Отправка выполняется асинхронно через Celery для улучшения производительности

- **Еженедельный дайджест (через Celery Beat)**
  - Автоматическая рассылка каждый понедельник в 8:00 утра
  - Список всех новых постов за последние 7 дней по категориям
  - Настраивается через Celery Beat в `config/settings.py`

### Фильтр цензуры

- **Кастомный темплейт-тег** (`news/templatetags/news_filters.py`)
  - Заменяет запрещённые слова на звёздочки (`*`), оставляя первую букву
  - Использование: `{{ text|censor }}`
  - При попытке применить фильтр не к строке выбрасывается `TypeError`

---

## 🏗️ Архитектура проекта

### Структура приложений

```
newsPortal/
├── config/          # Основные настройки проекта
├── news/            # Приложение новостей и статей
│   ├── models.py    # Модели: Post, Author, Category, Comment
│   ├── views.py     # Представления (CBV + миксины)
│   ├── forms.py     # Формы для создания/редактирования
│   ├── filters.py   # Фильтры для поиска
│   ├── signals.py   # Сигналы для email-уведомлений
│   └── templatetags/# Кастомные фильтры для шаблонов
├── sign/            # Регистрация и аутентификация
├── protect/         # Защищённые страницы
└── scheduler/       # Планировщик задач
```

### Модели данных

- **Author** - Профиль автора с рейтингом
- **Post** - Новости и статьи (различаются типом `post_type`)
- **Category** - Категории постов с подписчиками
- **Comment** - Комментарии к постам
- **PostCategory** - Связь многие-ко-многим между постами и категориями

### Миксины и переиспользование кода

Проект использует миксины для устранения дублирования:

- **`PaginationWindowMixin`** - Добавляет окно страниц для навигации
- **`PostCreateMixin`** - Общая логика создания постов с проверкой лимитов

---

## 🔧 Технические детали

### Зависимости

- **Django 5.2+** - Веб-фреймворк
- **django-allauth** - Аутентификация и OAuth
- **django-filter** - Фильтрация и поиск
- **Celery 5.3+** - Асинхронная очередь задач
- **Redis 5.0+** - Брокер сообщений для Celery
- **django-apscheduler** - Планировщик задач (опционально, для обратной совместимости)

### База данных

- По умолчанию: SQLite (`db.sqlite3`)
- Для продакшена рекомендуется PostgreSQL

### Настройки email

В `config/settings.py`:
- **Разработка**: `EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'`
- **Продакшен**: Настроить SMTP-сервер (пример в комментариях настроек)

---

## 📝 Примеры использования

### Создание новости

1. Войти в систему
2. Перейти на `/news/create/`
3. Заполнить форму (заголовок, текст, категории)
4. Сохранить

### Подписка на категорию

1. Перейти на страницу категории `/news/category/<id>/`
2. Нажать "Подписаться"
3. Получать уведомления о новых постах в этой категории

### Поиск

1. Перейти на `/news/search/`
2. Использовать фильтры:
   - По названию
   - По автору
   - По дате публикации

---

## 🛡️ Безопасность

- Все операции создания/редактирования/удаления требуют аутентификации
- Используются Django permissions для контроля доступа
- CSRF-защита включена по умолчанию
- ⚠️ **Важно**: Для продакшена необходимо вынести `SECRET_KEY` в переменные окружения

---

## 🔄 Недавние улучшения кода

- ✅ Устранено дублирование кода через миксины
- ✅ Исправлены импорты и структура кода
- ✅ Добавлена проверка прав доступа для удаления
- ✅ Исправлена генерация URL в сигналах
- ✅ Улучшена обработка preview-текста
- ✅ Оптимизированы запросы к БД (`select_related`, `prefetch_related`)
- ✅ **Интеграция Celery + Redis** для асинхронной обработки задач
- ✅ **Асинхронная рассылка уведомлений** через Celery
- ✅ **Автоматический еженедельный дайджест** через Celery Beat (каждый понедельник в 8:00)

---

## 📚 Дополнительная документация

- [Документация Django](https://docs.djangoproject.com/)
- [django-allauth](https://django-allauth.readthedocs.io/)
- [django-filter](https://django-filter.readthedocs.io/)



